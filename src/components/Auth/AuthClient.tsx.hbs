import React from "react";
import { Card, CardDescription, CardHeader, CardTitle } from "../ui/card";
import Image from "next/image";
import { cn } from "@/lib/utils";

type AuthClientProps = {
  cardClassName?: string;
  className?: string;
  children: React.ReactNode;
  cardTitle?: string;
  cardDescription?: string;
  title?: string;
  description?: string;
  image?: string;
};

const AuthClient: React.FC<AuthClientProps> = ({
  children,
  cardClassName,
  cardTitle,
  cardDescription,
  className,
  title,
  description,
  image,
}) => {
  return (
    <>
      <div
        className={cn(
          "w-full min-h-screen bg-background flex flex-col items-center justify-center relative",
          className
        )}
      >
        {image && (
          <Image
            src={image}
            className="z-[5] w-[98.5%] h-[55%] rounded-3xl absolute top-3 object-cover"
            alt=""
            width={1500}
            height={500}
          />
        )}
        {(title || description) && (
          <div className="z-[10] mb-16 mt-32 text-white w-[40rem] max-w-[95%] flex flex-col gap-3">
            <h1 className="text-6xl font-medium text-center">{title}</h1>
            <p className="text-center">{description}</p>
          </div>
        )}
        <Card
          className={cn(
            "w-[95%] md:w-[37rem] flex flex-col items-center z-[10] justify-center py-10 px-8 md:p-12 md:pt-5 mb-56 relative shadow-xl", !title && "mb-24",
            cardClassName
          )}
        >
          {(cardTitle || cardDescription) && (
            <CardHeader className="flex flex-col items-center justify-center gap-y-2 max-md:p-0">
              <CardTitle
                className={cn("text-4xl font-medium", title && "text-3xl")}
              >
                {cardTitle}
              </CardTitle>
              <CardDescription className="text-center">
                {cardDescription}
              </CardDescription>
            </CardHeader>
          )}
          {children}
        </Card>
        <div className="w-[40rem] max-w-[95%] absolute bottom-0">
          <h1 className="mt-24 mb-2 text-center">
            © 2024 {{ projectName }}. All Rights Reserved.
          </h1>
          <p className="text-center mb-5 text-foreground/30">
            Thank you for being a valued member of our community. We are
            committed to protecting your privacy and ensuring the security of
            your data.
          </p>
        </div>
      </div>
    </>
  );
};

export default AuthClient;
