import React from "react"; 
import { Input } from "../ui/input"; 
import Image from "next/image";
import { Search } from "lucide-react";

const Hero: React.FC = () => {
  return (
    <section className="h-[80vh] w-full bg-gradient-to-tr from-blue-400/10 from-[50%] to-blue-100/40 flex flex-col items-center justify-center gap-5 relative">
      <div className="absolute w-full h-[100%] top-0 left-0 bg-gradient-to-b from-white/90 to-transparent z-[5]"></div>
      <Image
        src={"/Books.jpg"}
        alt="Books"
        width={1920}
        height={1080}
        className="absolute inset-0 h-full w-full object-cover object-left z-[1] opacity-[0.5]"
      />

      {/* Header */}
      <header className="flex items-center justify-center z-[10] relative">
        <h1 className="text-5xl md:text-[4rem] md:leading-[4rem] font-black text-slate-800 text-center drop-shadow-md">
          Online <span className="text-primary">Public</span> <br /> Access
          Cataloguan
        </h1>
      </header>
      <div className="z-[5] relative flex flex-col items-center justify-center py-8 w-[90%] md:w-[60%] mt-10">
        <div className="bg-transparent bg-gradient-to-b from-white/80 to-sky-50/0 pointer-events-none w-[105%] h-[120%] absolute rounded-[2.5rem] backdrop-blur-md shadow-xl shadow-blue/10" />
        <div className="z-[5] relative w-full max-md:px-5">
          <h1 className="mr-auto mb-2 ml-4 font-medium text-xl text-slate-950 drop-shadow-sm">
            Apa yang anda cari?
          </h1>
          <Input
            suffix={<Search className="w-6 h-6 mr-1" strokeWidth={1.5} />}
            placeholder="Cari Disini"
            className="!bg-white focus:bg-white hover:bg-white w-[90%] md:!w-full !px-5 tracking-wide !text-base !rounded-2xl"
          />
        </div>
      </div>
      {/* <div className="flex gap-3">
        <Link className="underline" href="#">
          Pencarian Lanjut
        </Link>
        <Link className="underline" href="#">
          Riwayat Pencarian
        </Link>
        <Link className="underline" href="#">
          Bantuan
        </Link>
      </div> */}
    </section>
  );
};

export default Hero;
