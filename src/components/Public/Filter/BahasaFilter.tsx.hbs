"use client";
import { Card } from "@/components/ui/card";
import { Checkbox } from "@/components/ui/checkbox";
import { cn } from "@/lib/utils";
import { ChevronDown } from "lucide-react";
import React, { useState } from "react";

const BAHASA = [
  {
    name: "Indonesia",
    count: 5343,
  },
  {
    name: "Inggris",
    count: 324,
  },
];

const BahasaFilter: React.FC = () => {
  const [isOpen, setIsOpen] = useState(true);
  return (
    <div
      className={cn(
        `p-0 overflow-hidden transition-all duration-700 ease-out `,
        !isOpen ? "h-10" : "h-[8rem]"
      )}
    >
      <button
        onClick={() => setIsOpen(!isOpen)}
        className={"w-full flex items-center justify-between h-10 text-lg"}
      >
        <h1>Bahasa</h1>
        <ChevronDown />
      </button>
      {BAHASA.map((item, i) => (
        <div
          key={i}
          className={`w-full flex items-center py-2 px-5 `}
        >
          <Checkbox className="h-6 w-6 mr-2 rounded-md" />
          <h1>{item.name}</h1>
          <h1 className="ml-auto bg-info rounded-full px-2 text-xs text-background font-medium">
            {item.count}
          </h1>
        </div>
      ))}
    </div>
  );
};

export default BahasaFilter;
