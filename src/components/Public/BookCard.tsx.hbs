import React from "react";
import { Card } from "../ui/card";
import { Checkbox } from "../ui/checkbox";
import { Bookmark, CheckCircle2, Phone } from "lucide-react";
import { cn } from "@/lib/utils";
import Link from "next/link";

type BookCard = {
  gambar?: string;
  judul: string;
  jenis_bahan: "Monografi" | "Ebook" | "Buku";
  deskripsi: string;
  kreator: string;
  penerbitan: string;
  no_panggil: string;
  konten_digital: boolean;
  ketersediaan?: number;
  className?: string;
};

const BookCard: React.FC<BookCard> = ({
  gambar,
  jenis_bahan,
  judul,
  ketersediaan,
  deskripsi,
  konten_digital,
  kreator,
  no_panggil,
  penerbitan,
  className,
}) => {
  return (
    <Card
      className={cn(
        "w-full flex flex-col gap-5 border-none shadow-slate-200/80 shadow-md",
        className
      )}
    >
      <div className="w-full flex gap-5">
        <div className="flex gap-3">
          {/* <Checkbox className="mt-[1.3px]" />
        <Bookmark className="h-5 w-5" strokeWidth={1.3} /> */}
          <Card className="aspect-[3/4] h-[10rem] flex items-center justify-center text-center rounded-lg border-none shadow-slate-200 shadow-md bg-gradient-to-t from-slate-100/50 to-transparent text-sm">
            Sampul Belum Tersedia
          </Card>
        </div>
        <div className="w-full flex flex-col">
          <div className="flex justify-between items-center">
            <span className="text-xs tracking-normal text-slate-800">
              {kreator} - {penerbitan}
            </span>
            <Bookmark
              className="h-6 w-6 text-primary drop-shadow-md"
              strokeWidth={1.3}
            />
          </div>
          <h1 className="text-xl font-medium">{judul}</h1>

          <span className="text-xs tracking-normal mt-2 text-slate-800">
            {jenis_bahan} - {ketersediaan} pcs
          </span>
          <span className="text-slate-800 mt-3 font-light">
            <span className="md:hidden">
              {deskripsi?.substring(0, 100) || ""}
              {deskripsi?.length > 100 ? "..." : ""}
            </span>
            <span className="max-md:hidden">
              {deskripsi?.substring(0, 200) || ""}
              {deskripsi?.length > 200 ? "..." : ""}
            </span>
            <Link
              href="#"
              className="underline font-medium text-blue hover:scale-[1.1] transition-all duration-300 ease-out"
            >
              <br />
              Selengkapnya
            </Link>
          </span>
          <div className="w-full flex gap-3 mt-4 max-md:hidden">
            {konten_digital && (
              <span className="flex items-center justify-center w-fit gap-1 text-slate-800 text-sm font-medium">
                <CheckCircle2 className="h-4 w-4 text-blue" strokeWidth={2.5} />{" "}
                Konten Digital
              </span>
            )}
            {no_panggil && (
              <span className="flex items-center justify-center w-fit gap-1 text-slate-800 text-sm font-medium">
                <Phone className="h-4 w-4 text-primary" strokeWidth={2.5} />{" "}
                {no_panggil}
              </span>
            )}
            <span className="flex items-center justify-center w-fit gap-1 text-slate-800 text-sm font-medium">
              <Bookmark className="h-4 w-4 text-primary" strokeWidth={2.5} />{" "}
              342x
            </span>
          </div>
          {/* <div className="w-full flex">
          <h1 className="w-[25%]">Jenis Bahan</h1>
          <h1>: {jenis_bahan}</h1>
        </div>
        <div className="w-full flex">
          <h1 className="w-[25%]">Kreator</h1>
          <h1>: {kreator}</h1>
        </div>
        <div className="w-full flex">
          <h1 className="w-[25%]">Penerbitan</h1>
          <h1>: {penerbitan}</h1>
        </div>
        <div className="w-full flex">
          <h1 className="w-[25%]">No. Panggil</h1>
          <h1>: {no_panggil}</h1>
        </div>
        <div className="w-full flex">
          <h1 className="w-[25%]">Konten Digital</h1>
          <h1>: {konten_digital ? "Ada" : "Tidak Ada"}</h1>
        </div>
        <div className="w-full flex">
          <h1 className="w-[25%]">Ketersediaan</h1>
          <h1>
            : {ketersediaan[0]}/{ketersediaan[1]}
          </h1>
        </div> */}
        </div>
      </div>
      <div className="w-full flex gap-3 mt-4 md:hidden">
        {konten_digital && (
          <span className="flex items-center justify-center w-fit gap-1 text-slate-800 text-sm font-medium">
            <CheckCircle2 className="h-4 w-4 text-blue" strokeWidth={2.5} />{" "}
            Konten Digital
          </span>
        )}
        {no_panggil && (
          <span className="flex items-center justify-center w-fit gap-1 text-slate-800 text-sm font-medium">
            <Phone className="h-4 w-4 text-primary" strokeWidth={2.5} />{" "}
            {no_panggil}
          </span>
        )}
        <span className="flex items-center justify-center w-fit gap-1 text-slate-800 text-sm font-medium">
          <Bookmark className="h-4 w-4 text-primary" strokeWidth={2.5} /> 342x
        </span>
      </div>
    </Card>
  );
};

export default BookCard;
