"use client";
import { Button } from "@/components/ui/button";
import React from "react";

const Error = () => {
  return (
    <div className="flex flex-col items-center justify-center h-screen bg-red-100 text-destructive text-center p-5">
      <h1 className="text-6xl mb-5 font-medium">Oops!</h1>
      <p className="text-2xl mb-10">
        Something went wrong. Please try again later.
      </p>
      <Button
        variant={"destructive"}
        onClick={() => (window.location.href = "/")}
      >
        Go to Home
      </Button>
    </div>
  );
};

export default Error;
